export interface ModalidadDU {
  nIntCodigo: number;        // Código de la modalidad
  nIntClase: number;         // Clase (5001)
  cIntDescripcion: string;   // Descripción (PRESENCIAL, SEMIPRESENCIAL, A DISTANCIA)
  nIntTipo: number;          // Tipo (1)
}